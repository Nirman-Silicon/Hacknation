import React from 'react'

const addToCart = () => {
  return (
    <div>
      addToCart
    </div>
  )
}

export default addToCart


// const addToCart=(id, quantity)=>{
//     console.log(`added to cart with _id: ${id}, and quantity: ${quantity}`)
// }